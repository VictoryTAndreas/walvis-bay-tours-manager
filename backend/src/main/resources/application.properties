spring.application.name=backend

# 1. DATABASE CONNECTION (Using your Supabase Pooler)
# Note: Spring Boot requires the 'jdbc:postgresql://' prefix
spring.datasource.url=jdbc:postgresql://aws-1-eu-west-1.pooler.supabase.com:6543/postgres?sslmode=require
spring.datasource.username=postgres.gafhsbazwbgkjxuthtnr
spring.datasource.password=XJyJ2xagF0uO3BHc

# 2. DRIVER & DIALECT
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# 3. SCHEMA & DDL (Switching to 'none' or 'validate')
# Since Prisma handles migrations, we prevent Spring Boot from changing the tables.
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true

# 4. SECURITY
# Using your shared secret from the .env
app.jwt.secret=8e779bd3-9aee-474e-af2b-28154d88cc1c
app.jwt.expiration=3600000

spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl